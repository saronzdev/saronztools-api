{
	"version": 2,
	"functions": {
		"api/index.ts": { "runtime": "node@20" }
	},
	"installCommand": "pnpm install --no-frozen-lockfile",
	"rewrites": [
		{ "source": "/(.*)", "destination": "/api" }
	]
}