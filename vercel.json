{
	"version": 2,
	"functions": {
		"api/index.ts": { "runtime": "nodejs20.x" }
	},
	"installCommand": "pnpm install --no-frozen-lockfile",
	"rewrites": [
		{ "source": "/(.*)", "destination": "/api" }
	]
}